[package]
name = "pdftool-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "pdftool"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
pdftool-core = { path = "../pdftool-core" }
