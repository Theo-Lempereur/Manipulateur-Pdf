[package]
name = "pdftool"
version = "0.1.0"
edition = "2024"
description = "CLI tool for PDF manipulation using Ghostscript"
authors = ["Théo"]

[dependencies]
clap = { version = "4", features = ["derive"] }
